# Ubuntu bootstarp with dotfiles


#### Install ubuntu-bootstrap
 Run the following command on the Ubuntu box you wish to setup:
```bash
bash <(wget -O - https://raw.githubusercontent.com/maxkrivich/dotfiles/master/boostrap.sh)
```
